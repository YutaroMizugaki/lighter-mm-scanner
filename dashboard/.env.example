# Public HTTPS base for aggregate JSON only (no raw trades/books).
# Example (GCS public object URL prefix):
# NEXT_PUBLIC_DATA_BASE_URL=https://storage.googleapis.com/<BUCKET>/lighter-mm/public
NEXT_PUBLIC_DATA_BASE_URL=
